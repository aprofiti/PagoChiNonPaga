<%= form_for(@polo) do |f| %>
  <% if @polo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@polo.errors.count, "error") %> prohibited this polo from being saved:</h2>

      <ul>
      <% @polo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
