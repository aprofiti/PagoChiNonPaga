<%= form_for(@citta) do |f| %>
  <% if @citta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@citta.errors.count, "error") %> prohibited this citta from being saved:</h2>

      <ul>
      <% @citta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :provincia %><br>
    <%= f.text_field :provincia %>
  </div>
  <div class="field">
    <%= f.label :regione %><br>
    <%= f.text_field :regione %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
